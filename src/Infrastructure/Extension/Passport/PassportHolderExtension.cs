namespace Infrastructure.Extension.Passport
{
	public enum PassportHolderColumn
	{
		ConcurrencyStamp,
		CreatedAt,
		CultureName,
		EditedAt,
		EmailAddress,
		EmailAddressIsConfirmed,
		FirstName,
		Id,
		LastName,
		PhoneNumber,
		PhoneNumberIsConfirmed,
		SecurityStamp
	}

	public enum PassportHolderTable
	{
		PassportHolder
	}
}
